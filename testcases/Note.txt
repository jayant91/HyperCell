
//ALL Testcases here are done for the following configuration.
//Need to be Modified for a changed configuration

`define WORD_LENGTH 32
`define EXT_MEM_ADDR_WIDTH 32
`define CONFIG_BUS_WIDTH 32
//TODO: Note: The actual Fabric Configuration Data expected by the
//Fabric is 32 bit. But if the config bus width is 32, 1 bit is required for head/tail.
//So with this bus width, only 31 bits can be send as config tail. This is
//OK for now since, some of the MSBs are unused in Fabric Config. If this is
//not the case, the config bus width needs to be increased.

//Base addr + offset addr will form ext addr
`define BASE_ADDR_WIDTH 32
//Offset in Load/Str Lookup and Per loop Offset,
//need not be full width
`define OFFSET_ADDR_WIDTH 32

//Fabric parameters
`define   OUT_PORTS_20 //20 Output Ports used in Fabric Output Arbiter
//Pipelined mux only for 20 output ports
`ifdef OUT_PORTS_20
	`define NO_OF_OUT_PORTS 20
`else
	`define NO_OF_OUT_PORTS 18 //If not 20 give the number of ports
`endif 
`define NO_OF_INP_PORTS 20
`define COLSIZE 6


//`define LOCAL_STORE_SINGLE_WR_IF
`define SINGLE_FAB_OUT_CTRL
//`define ARBITE_FOR_LOAD_AND_FAB_OUT

//This is currently not implemented.
//`define SINGLE_LOAD_AND_FAB_OUT_ARBITE

`define NO_OF_LOAD_SEQ 1
`define NO_OF_FAB_INP_SEQ 1
`define NO_OF_STR_SEQ 1

`define LOC_STR_DEPTH 32
`define NO_OF_LOC_STR_BANKS 8
`define LOAD_SEQ_MEM_DEPTH 512
`define LOAD_REG_LKUP_DEPTH 60 //Each Load may need to be serviced by 3 lookups.
`define ADDR_LKUP_DEPTH 20
`define FAB_OUT_SEQ_MEM_DEPTH 64
`define FAB_INP_SEQ_MEM_DEPTH 64
`define STR_SEQ_MEM_DEPTH 512

//SEQ_MEM_DEPTH for atleast one of the sequencers should have 
//atleast same number of bits as Prologue and epilogue max depths.
`define PROLOGUE_MAX_WIDTH 7
`define EPILOGUE_MAX_WIDTH 7	 
`define STEADY_STATE_MAX_WIDTH 10	
`define EPILOGUE_SPILL_MAX_WIDTH 10	 
`define ITER_CNT_MAX_WIDTH 19 //No of bits

`ifdef LOCAL_STORE_SINGLE_WR_IF
	`define NO_OF_LOC_STR_WR_IF 1
`else
	`define NO_OF_LOC_STR_WR_IF `NO_OF_LOC_STR_BANKS
`endif

`ifdef SINGLE_FAB_OUT_CTRL
	`define OUT_DP_PER_CTRL `NO_OF_OUT_PORTS
	`define NO_OF_FAB_OUT_SEQ 1
`else
	`define OUT_DP_PER_CTRL 1
	`define NO_OF_FAB_OUT_SEQ `NO_OF_OUT_PORTS
`endif
